^:匹配开头,在中括号里面表示除了该字符的任意字符
$:匹配结尾
[] 用来匹配一个字符(一个)
		 [abc] 表示这个一位字符可以是a，也可以是b，也可以是c
		 [a-z] 表示这一位字符可以是[a,z]之间的任意的字母。
		 [a-zA-z]表示这一位任意字母。
		 [^abc] 表示这一位的字符可以是除了a,b,c的其他任意字符
		 [^0-9]表示除了数字以外的任意字符。
*:零个或多个
\ :转义字符。在正则和字符串都是转义字符，在java中，一般是双转义
.:小数点为通配符，可以匹配任意的字符。
\d:等价于0-9;用来匹配任意的数字
\D:用来匹配任意的非数字
\w:可以匹配任意额一个单词字符[a-zA-Z0-9_]
\W:可以匹配任意的一个非单词字符
+:表示前面的一位或者一组字符连续出现了一次或多次(一次及以上)
？：表示前面的一位或者一组字符连续出现一次或零次(零次或一次)
*:表示前面的一位或者一组字符连续出现了零次或多次(一次也可以匹配)
():表示一个分组
|：或
